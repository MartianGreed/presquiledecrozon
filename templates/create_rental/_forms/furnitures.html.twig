{{ form_start(form) }}
    {{ form_row(form.furnitures, { label: 'Quels équipements sont disponible dans votre logement ?' }) }}
    {{ form_row(form.customFurnitures, { label: 'Équipements supplémentaire' }) }}

    <div class="flex justify-end gap-4 mt-6">
        <button type="button" class="button button__outlined__grey" data-action="back#back">Retour</button>
        <button type="submit" class="button button__blue">Sauvegarder et continuer</button>
    </div>
{{ form_end(form) }}