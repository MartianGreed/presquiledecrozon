{{ form_start(form) }}
    {{ form_row(form.animalsAccepted, { label: 'Animaux acceptés' }) }}
    {{ form_row(form.smokingAllowed, { label: 'Logement fumeur' }) }}
    {{ form_row(form.additionnalRules, {
        label: 'Règles supplémentaires',
        attr: {
            'data-controller': 'form-types--collection',
        }
    }) }}

<div class="flex justify-end gap-4 mt-6">
    <button class="button button__outlined__grey">Retour</button>
    <button type="submit" class="button button__blue">Sauvegarder et continuer</button>
</div>
{{ form_end(form) }}