{% extends 'base.html.twig' %}

{% set content_class = 'profile' %}
{% block body %}
    <div class="profile__container">
        <div class="profile__sidebar">
            <div class="profile__sidebar__inner">
                <div class="profile__sidebar__inner__header">
                    {% if app.user.profile is not null and app.user.profile.picture is not null %}
                        <div class="profile__sidebar__inner__header__picture">
                            <img src="{{ uploaded_asset(app.user.profile.picture, 'file', null, { h: 200, w: 200 }) }}" alt="">
                        </div>
                    {% endif %}
                    {% if app.user.profile is not null and app.user.profile.firstname is not null and app.user.profile.lastname is not null %}
                    <div>{{ app.user.profile.firstname }} {{ app.user.profile.lastname }}</div>
                    {% endif %}
                    <div>Membre depuis {{ app.user.createdAt | date('Y') }}</div>
                </div>
                <ul class="profile__sidebar__inner__menu">
                    <li class="profile__sidebar__inner__menu-item">Messagerie</li>
                    <li class="profile__sidebar__inner__menu-item">Réservations</li>
                    <li class="profile__sidebar__inner__menu-item">Annonces</li>
                    <li class="profile__sidebar__inner__menu-item">Informations</li>
                </ul>
            </div>
        </div>
        <div class="profile__main">
            {% block profile_main '' %}
        </div>
    </div>
{% endblock %}