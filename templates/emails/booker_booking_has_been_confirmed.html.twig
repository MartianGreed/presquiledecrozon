{% extends 'emails/base.html.twig' %}

{% block email_content %}
    <row>
        <columns>
            <h1>Votre demande de réservation à bien été validée</h1>
        </columns>
    </row>
    <row>
        <columns>
            <button href="{{ url('app_homepage') }}" class="secondary">Secondary button</button>
        </columns>
        <columns>
            <button href="{{ url('app_homepage') }}">Classic button</button>
        </columns>
    </row>
{% endblock %}
