# This file describes an application. You can have multiple applications
# in the same project.

# The name of this app. Must be unique within a project.
name: app

# The type of the application to build.
type: php:8.1

build:
    flavor: composer

runtime:
    extensions:
        - pdo_pgsql
        - gd
        - imagick
        - curl


dependencies:
    php:
        composer/composer: '^2'
    nodejs:
        yarn: '*'

variables:
    env:
        # Tell Symfony to always install in production-mode.
        APP_ENV: 'prod'
        APP_DEBUG: 0
    
    php:
        memory_limit: '512M'
        date.timezone: 'Europe/Paris'

# The hooks that will be performed when the package is deployed.
hooks:
    build: |
        set -e
        yarn install
        yarn run build:prod
        chmod +x bin/deploydotenv.sh
        bin/deploydotenv.sh
        bin/console cache:clear
        bin/console app:assets:upload
    deploy: |
        set -e
        bin/console cache:clear
        bin/console doctrine:migrations:migrate --env=prod --no-interaction
        
# The relationships of the application with services or other applications.
# The left-hand side is the name of the relationship as it will be exposed
# to the application in the PLATFORM_RELATIONSHIPS variable. The right-hand
# side is in the form `<service name>:<endpoint name>`.
relationships:
    database: "db:postgresql"

# The size of the persistent disk of the application (in MB).
disk: 1024

# The mounts that will be performed when the package is deployed.
mounts:
    "/var/cache":
        source: local
        source_path: "cache"
    "/var/log":
        source: local
        source_path: "log"
    "/var/sessions":
        source: local
        source_path: "sessions"

# The configuration of app when it is exposed to the web.
web:
    locations:
        "/":
            # The public directory of the app, relative to its root.
            root: "public"
            # The front-controller script to send non-static requests to.
            passthru: "/index.php"
            
workers:
    messages:
        commands:
            start: bin/console messenger:consume async -vv --time-limit=3600 --memory-limit=128M
